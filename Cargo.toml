[package]
name = "trek-release"
version = "1.2.0"
edition = "2024"

[dependencies]
clap = { version = "4.6", features = ["derive"] }
zip = "2"
walkdir = "2"
globset = "0.4"

[profile.release]
opt-level = "z"
lto = true
codegen-units = 1
strip = true
panic = "abort"
